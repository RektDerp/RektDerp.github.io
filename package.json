{
  "name": "my-project",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
	"predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "parcel": "latest"
  },
  "dependencies": {
    "gh-pages": "^3.2.3",
    "parcel": "^2.0.0"
  }
}
